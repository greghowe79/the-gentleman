export interface Service {
  id: string;
  url: string;
  name: string;
  slug: string;
  price: number;
  category: string;
  created_at: string;
  description: string;
  sku: string;
  categorySlug: string;
}
