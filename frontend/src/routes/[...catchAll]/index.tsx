import { component$ } from '@builder.io/qwik';

// import { Image } from '@unpic/qwik';
// import { detailImage } from '../shop/styles.css';

const ProductDetail = component$(() => {
  /* { service }: any */
  return (
    <>
      {/* <Image
        src={service?.value[0]?.url}
        layout="constrained"
        decoding="async"
        loading="lazy"
        alt="A lovely bath"
        class={detailImage}
      /> */}
    </>
  );
});

export default ProductDetail;
